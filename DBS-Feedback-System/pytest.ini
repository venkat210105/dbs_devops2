[pytest]
addopts = -q
filterwarnings =
    ignore::pytest.PytestUnknownMarkWarning
# Restrict test discovery to orchestrator agents tests only
testpaths = orchestrator/agents/tests
norecursedirs = data docker-data .git node_modules target
markers =
    asyncio: mark a test as asyncio
